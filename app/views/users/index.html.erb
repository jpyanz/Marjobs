<h1>Users#index</h1>

<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.name+": "+user.username, user %>
        </li>
    <% end %>
</ul>

<%= link_to "New User", new_user_path, class: 'btn btn-primary' %>
