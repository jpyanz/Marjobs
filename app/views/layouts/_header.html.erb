<header class="sticky-top" role="banner">
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <a class="navbar-brand d-lg-none d-xl-none" href="/">Marjobs</a>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="#">Packages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Gallery</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                    <% if logged_in? %>
                        <li class="nav-item d-lg-none d-xl-none">
                            <%= link_to icon('fa-solid', 'right-from-bracket') +" Logout", logout_path, method: :delete, class: 'nav-link' %>
                        </li>
                    <% else %>
                        <li class="nav-item d-lg-none d-xl-none">
                            <%= link_to icon('fa-solid', 'right-to-bracket') +" Login", login_path, class: 'nav-link' %>
                        </li>
                    <% end %>
                </ul>
            </div>

            <a class="navbar-brand d-none d-lg-block d-xl-block" href="/">Marjobs</a>

            <div class="collapse navbar-collapse justify-content-end">
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <% if logged_in? %>
                        <!-- 
                        <li class="nav-item">
                            <%= link_to "Users", users_path, class: 'nav-link' %>
                        </li>
                        -->
                        <li class="nav-item">
                            <%= link_to icon('fa-solid', 'right-from-bracket') +" Logout", logout_path, method: :delete, class: 'nav-link' %>
                        </li>
                    <% else %>
                        <li class="nav-item">
                            <%= link_to icon('fa-solid', 'right-to-bracket') +" Login", login_path, class: 'nav-link' %>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>