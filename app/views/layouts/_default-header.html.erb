<header role="banner">
    <nav class="navbar fixed-top bg-white navbar-expand-lg">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-between w-100" id="navbarToggler">
                <div class="d-flex justify-content-between align-items-center flex-wrap">
                    <a class="navbar-brand" href="/"><%= image_tag "logo.png", :class => "img-fluid" %></a>
                    <div class="d-flex d-lg-none">
                        <%= link_to "SCHEDULE A MEETING", checkout_new_path, class: 'btn btn-primary', type: 'button' %>
                        <% if logged_in? %>
                            <div class="dropdown">
                                <button class="profile-btn btn btn-link dropdown-toggle text-decoration-none" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <span class="profile-pic rounded-circle text-bg-secondary"><%= icon('fa-solid', 'user') %></span>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <%= link_to "Dashboard", bookings_path, method: :delete, class: 'dropdown-item' %>
                                    </li>
                                    <li>
                                        <%= link_to icon('fa-solid', 'right-from-bracket') +" Sign out", logout_path, method: :delete, class: 'dropdown-item' %>
                                    </li>
                                </ul>
                            </div>
                        <% else %>
                            <%= link_to icon('fa-solid', 'right-to-bracket') +" Sign in", login_path, class: 'btn btn-primary ms-2', type: 'button' %>
                        <% end %>
                    </div>
                </div>

                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a href="/" class="nav-link">Home</a>
                    </li>
                    <li class="nav-item">
                        <%= link_to "About", about_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Packages", checkout_path, class: "nav-link" %>
                    </li>
                    <!--
                    <li class="nav-item">
                        <%= link_to "Gallery", gallery_path, class: "nav-link" %>
                    </li>
                    -->
                    <li class="nav-item">
                        <%= link_to "Contact", contact_us_path, class: "nav-link" %>
                    </li>
                </ul>

                <div class="d-flex d-none d-lg-flex">
                    <%= link_to "SCHEDULE A MEETING", checkout_new_path, class: 'btn btn-primary', type: 'button' %>
                    <% if logged_in? %>
                        <div class="dropdown">
                            <button class="profile-btn btn btn-link dropdown-toggle text-decoration-none" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <span class="profile-pic rounded-circle text-bg-secondary"><%= icon('fa-solid', 'user') %></span>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li>
                                    <%= link_to "Dashboard", bookings_path, method: :delete, class: 'dropdown-item' %>
                                </li>
                                <li>
                                    <%= link_to icon('fa-solid', 'right-from-bracket') +" Sign out", logout_path, method: :delete, class: 'dropdown-item' %>
                                </li>
                            </ul>
                        </div>
                    <% else %>
                        <%= link_to icon('fa-solid', 'right-to-bracket') +" Sign in", login_path, class: 'btn btn-primary ms-2', type: 'button' %>
                    <% end %>
                </div>
            </div>
        </div>
    </nav>
</header>